pipeline {
    agent any
    stage('Build') {
        steps {
            // sh 'npm install'
            // sh 'npm run build'
            echo "Build"
        }
    }
    stage('Test') {
        steps {
            // sh 'npm run test'
            echo "Test"

        }
    }
    stage('Build Image') {
        steps {
            echo "Build Image"
        }
    }
    stage ('Deploy') {
        steps {
            echo "Deploy"
        }
    }
}
